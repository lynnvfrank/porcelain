# Example Continue configuration for Locus Gateway.
# Merge into ~/.continue/config.yaml or workspace .continue/config.yaml.
#
# Continue v1: use one `models` list entry per dropdown item (not a separate `providers` block
# for OpenAI-compatible). Share `apiBase` / `apiKey` via a YAML anchor — see below.
#
# Each `model` value must match an `id` from GET <gateway>/v1/models (virtual locus-*
# plus upstream ids: BiFrost provider/model or LiteLLM model_name aliases).

%YAML 1.1
---
name: Porcelain
version: "1.0.0"
schema: v1

models:
  - name: Locus
    model: locus-0.2.0
    provider: openai
    apiKey: <GATEWAY_TOKEN>
    apiBase: <LOCUS_GATEWAY_URL>
    # Attach gateway headers for RAG + log correlation (Continue schema varies by version).
    # Uncomment and adapt one of the shapes below; keys often appear under requestOptions or similar.
    #
    # requestOptions:
    #   headers:
    #     X-Locus-Project: my-project
    #     X-Locus-Flavor-Id: ""
    #     X-Locus-Conversation-Id: "<uuid-or-thread-id>"
    roles:
      - chat
      - edit
      - apply
    capabilities:
      - reasoning
      - tool_use
