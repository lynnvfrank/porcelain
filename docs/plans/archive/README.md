# Archived plans

Completed delivery history (`shipped`, `done`, or `superseded`). These files preserve rationale and phase notes.

**Do not use this directory as a work queue.** For current behavior, read [`docs/features/`](../../features/README.md). For open work, read [`../README.md`](../README.md).

Plans are moved here when their front-matter **Status** is no longer `draft` or `active`.
